# Architecture

High-level architecture for the Model Registry scaffold. Backend is a FastAPI
service, frontend is a React app, and data fetching/metrics are modularized in
`datafetchers/` and `metrics/` packages.
